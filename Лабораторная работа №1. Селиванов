# Целые нечетные числа, начинающиеся с 22. Выводит на экран нечетные числа, после каждого числа добавляя через тире, сколько раз в нем встретилась введенная с клавиатуры цифра. Цифра выводится прописью
d = {'0': 'ноль', '1': 'один', '2': 'два', '3': 'три', '4': 'четыре', 
     '5': 'пять', '6': 'шесть', '7': 'семь', '8': 'восемь', '9': 'девять'}

c = input("Цифра: ")

with open('input.txt') as f:
    lex = ''
    for char in f.read():
        if char.isdigit():
            lex += char
        elif lex:
            if lex.startswith('22') and int(lex) % 2:
                print(f"{lex}-{d[str(lex.count(c))]}")
            lex = ''
